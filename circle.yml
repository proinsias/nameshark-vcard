---
machine:
  python:
    version: 3.5.2

dependencies:
  pre:
    - pip install pytest
    - pip install pytest-circleci

test:
  override:
    - python -m pytest

  post:
    - curl -sSL https://download.sourceclear.com/ci.sh | sh
